Administration Guide
====================

This guide is aimed to day-to-day administration of the data in koji (for
system-related tasks consult :doc:`../Operations Guide/index` instead.
Nevertheless, some tasks are on the border of these two, so look also to the
other guide if you don't find the answers here.

.. toctree::
   :maxdepth: 1

   access_controls
   permissions
   defining_hub_policies
   exporting_repositories
   setting_rpm_macros
   signing
   tag_inheritance
   winbuild
   misc
